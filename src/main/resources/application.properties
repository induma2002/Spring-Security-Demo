spring.application.name=${SPRING_APP_NAME:springsecsection1}

# Customize the log pattern for the console with colors
#logging.pattern.console=${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}

# These credentials will be replaced by environment variables
#spring.security.user.name = ${SECURITY_USERNAME:induma}                ////// we try to make multiple users
#spring.security.user.password = ${SECURITY_PASSWORD:12345}             ////// we try to make multiple users

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/eazybank}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}

spring.jpa.hibernate.ddl-auto=
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Spring Security logging level set via environment variable
logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:trace}
